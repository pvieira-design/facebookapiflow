{
  "buildCommand": "npm run db:generate --workspace @faceapiads/db && npm run build --workspace web",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next"
}
